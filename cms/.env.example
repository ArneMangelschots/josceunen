HOST=0.0.0.0
PORT=1337

# Public URL of this Strapi instance (used for media absolute URLs)
PUBLIC_URL=http://localhost:1337

# Comma-separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

APP_KEYS=toBeReplaced1,toBeReplaced2
API_TOKEN_SALT=tobereplaced
ADMIN_JWT_SECRET=tobereplaced
TRANSFER_TOKEN_SALT=tobereplaced
JWT_SECRET=tobereplaced

DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db
